# 🚀 Как запустить Jupyter Notebook локально

## Проблема: "Не могу запустить notebook"

### ✅ Решение пошагово:

## Шаг 1: Выберите правильный Kernel в VS Code

1. **Откройте файл** `notebooks/vieta_interactive.ipynb` в VS Code
2. **В правом верхнем углу** найдите кнопку выбора kernel (может быть написано "Select Kernel" или показан текущий Python)
3. **Нажмите на неё** и выберите **"Python (Vieta)"** из списка
   - Если не видите "Python (Vieta)", выберите "Python 3.12.6" из `.venv`
   - Полный путь: `/Users/kate/Downloads/Novikhin/.venv/bin/python`

## Шаг 2: Установите Jupyter extension для VS Code (если нужно)

1. Откройте Extensions (⌘+Shift+X на Mac)
2. Найдите "Jupyter"
3. Установите официальное расширение от Microsoft

## Шаг 3: Запустите ячейки

После выбора kernel:
- Нажмите ▶️ рядом с ячейкой чтобы запустить её
- Или нажмите **Shift+Enter** для запуска и перехода к следующей ячейке

## Если всё равно не работает:

### Вариант A: Запуск через Jupyter Lab

```bash
# Активируйте окружение
source .venv/bin/activate

# Запустите Jupyter Lab
jupyter lab notebooks/vieta_interactive.ipynb
```

Откроется в браузере на http://localhost:8888

### Вариант B: Запуск через Jupyter Notebook (классический)

```bash
# Активируйте окружение  
source .venv/bin/activate

# Запустите классический Jupyter Notebook
jupyter notebook notebooks/vieta_interactive.ipynb
```

### Вариант C: Google Colab (ПРОЩЕ ВСЕГО!)

Просто откройте ссылку в браузере - ничего устанавливать не нужно:

[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/moyamoyaradost/registration-form/blob/main/notebooks/vieta_interactive.ipynb)

---

## 🔧 Технические детали

### Что было сделано:

1. Установлен `ipykernel` в виртуальное окружение
2. Зарегистрирован kernel "Python (Vieta)" для Jupyter
3. Все необходимые библиотеки установлены:
   - numpy
   - matplotlib  
   - ipywidgets
   - plotly
   - ipython
   - jupyter

### Проверка установки:

```bash
# Проверить список kernels
jupyter kernelspec list

# Должен показать:
# vieta-env    /Users/kate/Library/Jupyter/kernels/vieta-env
```

---

## ❓ FAQ

**Q: Почему кнопка "Run" не работает?**  
A: Убедитесь что выбран kernel (см. Шаг 1)

**Q: Вылетает ошибка "ModuleNotFoundError"?**  
A: Проверьте что выбран правильный kernel из .venv, а не системный Python

**Q: Ячейка выполняется бесконечно?**  
A: Нажмите кнопку остановки (⏹️) и перезапустите kernel

**Q: Лучший способ для обучения?**  
A: **Google Colab** - не требует настройки, работает везде!

---

## 💡 Рекомендация

Для **быстрого старта** используйте **Google Colab**:
- Открывается в один клик
- Все библиотеки устанавливаются автоматически  
- Работает на любом устройстве (телефон, планшет, компьютер)
- Можно сохранить в Google Drive

Для **серьёзной работы** используйте **локальный Jupyter** в VS Code:
- Быстрее работает
- Можно работать без интернета
- Больше контроля над окружением
